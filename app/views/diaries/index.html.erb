<% if user_signed_in? %>
  <p id="notice"><%= notice %></p>

  <div class="current-name">
    <%= "#{current_user.nickname}さんの状態" %>
  </div>
  <% if @diaries.present? %>
  <%= "体力：#{@sleep_average + @meal_average + @motion_average}点" %>
  <%= "技術：#{@study_average + @output_average + @today_average}点" %>
  <%= "精神：#{@margin_average + @tired_average + @refresh_average}点" %>
  <% end %>
  <table>
    <thead>
      <tr>
        <th>タイトル</th>
        <th>時間</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @diaries.each do |diary| %>
        <tr>
          <td><%= diary.sentence %></td>
          <td><%= diary.start_time.strftime("%Y-%m-%d %H:%M") %></td>
          <td><%= link_to 'Show', diary %></td>
          <td><%= link_to 'Edit', edit_diary_path(diary.id) %></td>
          <td><%= link_to 'Destroy',diary_path(diary.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to '日記をかく', new_diary_path %>

  <%= month_calendar events: @diaries do |date, diaries| %>
    <%= date.day %>

    <% diaries.each do |diary| %>
      <div>
        <%= link_to '日記を見る', diary %>
      </div>
    <% end %>
  <% end %>
<% else %>
<div>
  <%= image_tag "人混み.jpg", class:"main-image" %>
</div>
<h1 class="htb-main">ハテボチェッカーとは？</h1>
<div class="htb-sub">
<p>近年、ストレス社会と呼ばれるくらい自分をコントロールすることが難しい世の中になりました。</p>
<p>そこで、ハテボチェッカーを利用することで自分の体力・技術・精神がどれくらいなのかを点数し、自分の状態を客観的に見る事ができます！</p>
</div>
<p class="htb-st3">ハテボチェッカーの利用方法は、簡単３ステップです！</p>
<ul class="htb-step">
  <li>
    <img src="/assets/会員登録.jpg" alt="会員登録" class="step-image">
    <p class="step-name">①会員登録する</p>
  </li>
  <li>
    <img src="/assets/日記.jpg" alt="日記" class="step-image">
    <p class="step-name">②日記を書く</p>
  </li>
  <li>
    <img src="/assets/日記確認.jpg" alt="日記確認" class="step-image">
    <p class="step-name">③トップページで今の状態を確認する！</p>
  </li>
</ul>
<p class="htb-sub3">日記を見るボタンをクリックすれば日記の詳細（その日の状態）を見ることできます。また、日記は後で修正することもできますし、消すこともできます！</p>

<p class="htb-sub4">さぁ！ハテボチェッカーを試してみよう！</p>
<%= link_to '新規登録', new_user_registration_path, class: :nav__btn %>
<% end %>
